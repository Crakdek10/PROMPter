# PROMPter Backend

Backend de PROMPter (FastAPI). Hito 0: servidor + /health.

## Requisitos
- Python 3.13+

## Setup
Desde la raíz del monorepo:

```powershell
py -3.13 -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r backend/requirements.txt
